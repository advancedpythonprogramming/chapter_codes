# We create a list with 3 elements
songs = ['Addicted to pain', 'Ghost love score', 'As I am']
print(songs)

# Then, we add the list "songs" to the list "new_songs"
new_songs = ['Elevate', 'Shine']
songs.extend(new_songs)
print(songs)
