# 4.py

# IndexError exception: index out of range. A typical error that ocurrs
# when we try to access to an element of a list with an index that exceeds
# its size.
# Lists in Python have indexes from 0 to len(list_)-1

age = [36, 23, 12]
print(age[3])
